<div class="container">
  <ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task.id) %></li>
    <li><%= task.details %></li>
  <% end %>
  </ul>
</div>

<div class="container">
  <%= link_to "Add a new task", new_task_path %>
</div>
